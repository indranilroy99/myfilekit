1. [BLOCKER] src/App.tsx:1077 — Drag-to-reorder is HTML5 `draggable` + onDragStart/onDragOver/onDrop only, which is pointer/touch-only; a keyboard user cannot reorder files. For Merge, list order = output page order, so an essential function is keyboard-inoperable (the grip at :1084 is `aria-hidden` and the `<li>` has no tabindex/keydown). Fix: add per-chip up/down move buttons (min 44px, `aria-label="Move {name} up/down"`, disabled at ends) — or make each `<li>` a roving-tabindex element with ArrowUp/ArrowDown handlers calling `reorder()` — and add an `aria-live` announcement of the new position; keep the mouse drag as an enhancement.

2. [MAJOR] src/App.tsx:928 — When `downloadReady` is set the success card renders but `showStatusBox` is false (App.tsx:921), so the only `role=status`/`aria-live=polite` region (StatusBox) is not rendered and the `.result-card` is plain `<div>`s with no role, no `aria-live`, and no focus move. Screen-reader users get no notification that the export finished. Fix: wrap the result card (or its "Done — ready to save" heading) in `role="status" aria-live="polite"`, or move focus to the card heading (`tabIndex={-1}` + `.focus()`) when it appears.

3. [MAJOR] src/styles.css:1007 — `.dropzone-cta` uses `--primary-foreground` (#fafafa) on `--primary`; in dark theme `--primary` = #3b82f6, giving 3.52:1 for 13px/800 text — below the 4.5:1 normal-text minimum (light theme #2563eb = 4.95:1, passes). The same combo hits the `.primary-button` "Download" (App.tsx:937), the real result-card action. Fix: darken dark-theme `--primary` (e.g. #2563eb → ~5:1) or set a darker `--primary-foreground` for buttons/CTA in dark.

4. [MAJOR] src/styles.css:1051 — `.file-chip-remove` is 28×28px, violating invariant 3 (interactive targets ≥ 44px); it passes WCAG 2.5.8 (24px) but not the project floor, and is the only way to deselect a file. Fix: set `width/height: 44px` (keep the 15px icon visually centered), or give it `min-height:44px;min-width:44px` with padding.

5. [MINOR] src/App.tsx:934 — Result-card metadata line uses Tailwind `text-neutral-500` (#6b7280) on the light green-tint card background (rgba(34,197,94,.06) over #f7f8fb ≈ rgb(234,245,242)) = 4.34:1 for 12px normal text — under 4.5:1. Fix: use `var(--foreground)`/a darker token or `--success-fg` (#31412f) for this line.

6. [MINOR] src/App.tsx:1086 — Removing a file destroys the focused `.file-chip-remove` button and re-renders the list, so keyboard focus is dropped to `<body>`; there is also no `aria-live` announcing "file removed" or the resulting count. Fix: after removal move focus to the previous chip's remove button (or the dropzone), and add a visually-hidden `aria-live="polite"` region announcing count changes ("2 files selected", "file removed").

7. [MINOR] src/styles.css:969 — The dropzone's only keyboard focus indicator is the `:focus-within` border-color change (identical to `:hover`); the actually-focused element is the `sr-only` `<input>` whose global 2px `:focus-visible` ring (styles.css:185) is clipped to 1px and invisible. Invariant 3 asks for a visible focus ring on every target. Fix: add a distinct focus style to the label on `:focus-within` (e.g. `outline: 2px solid var(--ring); outline-offset: 3px`) so keyboard focus differs from hover.

## Resolutions
1. FIXED — up/down move buttons (keyboard + touch), aria-labelled, disabled at ends; drag kept as enhancement.
2. FIXED — result card is `role="status" aria-live="polite"`; plus an sr-only `aria-live` region announces the selected-file count.
3. FIXED — dark `.primary-button` and `.dropzone-cta` → #2563eb (≈4.95:1) (verified in built CSS).
4. FIXED — remove is 44×44 via `.icon-button`.
5. RESOLVED by de-greening — result meta now sits on `--card`/slate, not the green tint.
6. FIXED — focus returns to the file input after removal; sr-only live region announces the new count.
7. FIXED — distinct `:focus-within` outline ring on the dropzone.
