1. [BLOCKER] src/styles.css:1071,1073,1084,1102 — the result card is a fully green treatment (border `var(--success)`, tint `rgba(34,197,94,.06)`, green check icon), which hard-fails invariant 1 (single blue accent, no multi-color decoration); worse, the border/check use the sage token `--success` (#b9c6a7) while the background hardcodes an off-palette vivid green (#22c55e), so two unrelated greens sit in one card. Fix: drop the green entirely — render the card with the existing muted surface + `--primary` for the check and any emphasis, so success reads through the "Done — ready to save" label and icon shape, not a color no other surface uses.
2. [BLOCKER] src/styles.css:1053-1058 (App.tsx:1086) — `.file-chip-remove` is a 28x28px button, below the 44px minimum in invariant 3, and it is the only control on each file row. Fix: give it a >=44x44px hit area (keep the 28px glyph, add padding/negative margin so the tap target reaches 44px) so it is usable on touch.
3. [MAJOR] src/App.tsx:1077 (styles.css:1031) — reorder is wired only to native HTML5 `draggable`, and the grip is a non-focusable `<span>`; native drag-and-drop does not fire on touch devices and there is no keyboard path, so on mobile and for keyboard users the file order (page order for Merge) cannot be changed at all. Fix: add explicit move-up/move-down buttons (or arrow-key handling on a focusable row control) as the primary reorder mechanism and keep pointer drag as a progressive enhancement.
4. [MAJOR] src/App.tsx:1071-1084 (styles.css:1027) — drag-reorder gives no discoverability or drop feedback: the only signal is `opacity:.5` on the lifted chip, there is no insertion indicator showing where it will land and no hint that chips are reorderable, so users won't know the feature exists or where the drop will go. Fix: show an insertion line on the drag-over target and add a one-line "Drag to reorder" hint above the list when `multiple`.
5. [MAJOR] src/App.tsx:933 (styles.css:1080) — the filename is `font-black` (900) while its own card headline "Done — ready to save" is 800, inverting hierarchy so the filename shouts louder than the status it sits under; combined with 800 on `.dropzone-title`/`.dropzone-cta` this is the font-weight overuse invariant 4 warns against. Fix: make the headline the single heaviest weight in the card and drop the filename to ~600-700; reserve 800+ for one element per block.
6. [MINOR] src/App.tsx:1069 (styles.css:1000-1008) — "Browse files" is styled as a solid primary button but is a non-focusable `<span>` at 36px min-height; keyboard focus actually lands on the visually-hidden input, so the button-looking element is not the real control and undershoots 44px. Fix: either make it the genuine, focusable affordance sized >=44px with the label's focus ring visibly wrapping it, or demote it to plain link-style text so it doesn't imply a separate button.
7. [MINOR] src/styles.css:969-972 — the dropzone's keyboard focus state (`:focus-within`) only recolors the border to `--primary`, identical to `:hover`, so focus is ambiguous and there is no distinct ring as invariant 3 expects. Fix: add a separate `:focus-within` outline/box-shadow in `--primary` with offset, distinct from the hover border tint.
8. [MINOR] src/App.tsx:957,934,1068 — privacy microcopy is redundant and slightly shouty: the always-on trust line "your files never leave this device" duplicates the result card's "stayed on this device" (both visible together on the done state), and `acceptHint`'s "· add several" is rendered by the uppercased `.dropzone-hint` as "ADD SEVERAL", reading as a shouted command. Fix: suppress the trust line when the result card is shown, and move the multi-file cue out of the uppercased hint (e.g. into the CTA or a normal-case sublabel).

## Resolutions
1. FIXED — de-greened: border `--line`, bg `--card`, check `--primary`. Success now reads via the check + "Done" label (single accent).
2. FIXED — remove is now `.icon-button` (44×44, verified in built CSS).
3. FIXED — added keyboard/touch up/down move buttons (44px, aria-labelled, disabled at ends); pointer drag kept as enhancement.
4. PARTIAL — visible up/down buttons are now the primary reorder path + a "Reorder with the arrows (or drag)" hint. Insertion-line indicator declined (buttons make drag optional; low added value).
5. FIXED — filename → font-semibold; the "Done…" head (800) is now the heaviest weight in the card.
6. PARTIAL — the label now shows a distinct `:focus-within` ring (real control = the label/input). CTA kept as a visual affordance; declined making it a second focusable button to avoid dual focus targets.
7. FIXED — distinct `:focus-within` outline ring, separate from the hover border tint.
8. FIXED — trust line suppressed when the result card shows; "· add several" removed from the uppercased hint.
